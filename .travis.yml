language: python

dist: trusty
sudo: false

python:
    - '2.7'
    - '3.4'
    - '3.5'
    - '3.6'

matrix:
    include:
        - python: '3.7'
          dist: xenial
          sudo: true

cache: pip
install: make
script: make ci
